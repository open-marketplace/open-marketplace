{% if data.isClosed() == false %}
    <form action="{{ path('open_marketplace_admin_messaging_conversation_archive', { id: data.id }) }}" method="post">
        <input type="hidden" name="_method" value="POST">
        <button class="ui grey labeled icon button" type="submit" data-requires-confirmation {{ sylius_test_html_attribute('archieve-button') }}>
            <i class="icon archive"></i> {{ action.label|trans }}
        </button>
        <input type="hidden" name="_csrf_token" value="{{ csrf_token(data.id) }}" />
    </form>
{% endif %}
