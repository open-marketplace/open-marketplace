{% set product = product_review.reviewSubject %}

<h4 class="ui top attached styled header">
    {{ 'sylius.ui.product'|trans }}
</h4>
<div class="ui attached segment">
    <a href="{{ path('open_marketplace_vendor_product_listing_edit_product', {'id': product.id}) }}" class="header sylius-product-name">{{ product.name }}</a>
</div>
