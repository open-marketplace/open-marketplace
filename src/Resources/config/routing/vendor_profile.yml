vendor_profile:
    path: /vendor/profile
    methods: [GET]
    defaults:
        _controller: open_marketplace.controller.vendor_controller:showAction
        _sylius:
            template: "Vendor/profile.html.twig"
