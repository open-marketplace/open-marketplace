{% if vendor.editDate is not null %}
    <span class="ui red label"><i class="exclamation icon"></i>{{ 'Vendor requested changes on ' ~ vendor.editDate }}</span>
{% endif %}
<h4 class="ui dividing header">{{ 'bitbag_mvm_plugin.ui.vendor_details'|trans }}</h4>
<div class="two fields">
    {{ form_row(form.companyName) }}
    {{ form_row(form.taxIdentifier) }}
</div>
{{ form_row(form.phoneNumber) }}
