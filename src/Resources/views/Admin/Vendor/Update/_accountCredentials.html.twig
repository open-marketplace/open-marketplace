<h4 class="ui dividing header">{{ 'sylius.ui.account_credentials'|trans }}</h4>
{% if vendor.companyName is empty or vendor.companyName is not null %}
    <div class="field">
        <div class="ui toggle checkbox">
{#            {{ form_row(form.createUser) }}#}
            <div>IAMHERE</div>
        </div>
    </div>
{% endif %}
{% if form.vendor is defined %}
    <div id="user-form" {% if vendor is not defined %} style="display: none" {% endif %}>
{#        {{ form_row(form.user.plainPassword) }}#}
{#        {{ form_row(form.user.enabled) }}#}
{#        {{ form_row(form.user.verifiedAt) }}#}
    </div>
{% endif %}
