<h4 class="ui dividing header">{{ 'sylius.ui.account_credentials'|trans }}</h4>
{% if form.vendor is not defined %}
    <div id="user-form" {% if vendor is not defined %} style="display: none" {% endif %}>
    </div>
{% endif %}
