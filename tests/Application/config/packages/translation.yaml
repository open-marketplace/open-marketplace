framework:
    default_locale: '%locale%'
    translator:
        paths:
            - '%kernel.project_dir%/../../src/MVM/translations'
            - '%kernel.project_dir%/translations'
        fallbacks:
            - '%locale%'
            - 'en'
